#
# ssh/known_hosts の作り方
#	ssh -6 localhost -o UserKnownHostsFile=ssh/known_hosts
#
# ssh/identity(.pub) の作り方
#	ssh-keygen -b 4096 -f ssh/identity -C "$USER@console"
#
# authorized_keys への登録方法
#	cat ssh/identity >> ~/.ssh/authorized_keys
#
